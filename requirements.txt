# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (async)
httpx>=0.27.0

# Data validation / settings
pydantic>=2.7.0
pydantic-settings>=2.2.0

# TOML parsing for pyproject.toml (Python < 3.11 backport)
tomli>=2.0.1; python_version < "3.11"

# YAML parsing — used to read product_version.yaml from analysed repos
pyyaml>=6.0

# Excel export (alert reports)
openpyxl>=3.1.0

# PDF export (alert reports)
reportlab>=4.0

# OpenAI SDK (optional — for LLM-assisted reachability analysis)
openai>=1.30.0

# Jira (optional — direct library alternative to raw httpx calls)
# jira>=3.8.0

# Development / testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-httpx>=0.30.0
respx>=0.21.0
python-dotenv>=1.0.0
